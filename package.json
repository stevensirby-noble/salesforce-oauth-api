{
  "name": "salesforce-oauth-api",
  "version": "1.0.0",
  "description": "API for Salesforce OAuth and Stripe integration",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "stripe": "^14.0.0",
    "crypto": "^1.0.1"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  }
}
